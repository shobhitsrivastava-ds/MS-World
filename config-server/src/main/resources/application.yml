server:
  port: 8084


spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shobhitsrivastava-ds/Config-Server.git
            clone-on-start: true
